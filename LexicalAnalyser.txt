#include <stdio.h>
int maximum(int x, int y) {
	// This will compare 2 numbers
    if (x > y)
        return x;
    else {
        return y;
    }
    /* this should
    not be considered
    ever */
    int a = "this" this;
    g == +a + -b * a / b % c != gh;
    a = bsah++ + cc - 4.3;
    b = ++bsah + cc;
    if ((ab || cd) && (gh >= hi))
    	pqrs >> **4 + &b;
    print("GotoGFG %d %d", a);
}